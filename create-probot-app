#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/node_modules/create-probot-app/bin/create-probot-app.js" "$@"
  ret=$?
else 
  node  "$basedir/node_modules/create-probot-app/bin/create-probot-app.js" "$@"
  ret=$?
fi
exit $ret
